{% extends 'base.html.twig' %}

{% block title %}Editer Client
{% endblock %}

{% block body %}
	{% include 'partials/navbar.html.twig' %}

	<h1 class="text-center mt-5">Modification de clients</h1>
	<div class="container">

		{{ form_start(form) }}
		<div class="md-4" style="margin-left: 27em;">
			{{ form_widget(form.anarana) }}
			<button class="btn btn-primary">{{ button_label|default('Editer') }}</button>
			<button class="btn btn-primary">{{ button_label|default('Supprimer') }}</button>

		</div>


		{{ form_end(form) }}


		<a href="{{ path('app_client_index') }}" class="btn">Consulter la liste</a>

		{{ include('client/_delete_form.html.twig') }}
	{% endblock %}
